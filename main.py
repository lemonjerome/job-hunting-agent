"""
Job Hunting Agent — Entry point.

Usage:
    python main.py

The compiled LangGraph will be imported and invoked here once Phase 7 is complete.
"""

import asyncio


async def main() -> None:
    # Phase 7: import and run the compiled graph
    # from graph.graph import graph
    # result = await graph.ainvoke({})
    # print(result)
    print("Job Hunting Agent — scaffold ready. Implement phases 1–7 before running.")


if __name__ == "__main__":
    asyncio.run(main())
